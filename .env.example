# Copy to .env for local overrides.
#
# HOST is the load-bearing one: Nitro defaults to 0.0.0.0, which would break the
# local-only invariant. Never change this to a routable address.
HOST=127.0.0.1
PORT=4321
